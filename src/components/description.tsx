import styled from "styled-components";

export default styled.p`
    color: ${({ theme }) => theme.colors.description};
    margin-left: 10px;
    margin-right: 10px;
    text-align: center;
    font-size: 1.5rem;
`;
